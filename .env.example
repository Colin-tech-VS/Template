# Exemple de configuration des variables d'environnement

# ==============================================
# CLÉS API ET SÉCURITÉ
# ==============================================
# Clé API maître pour permettre au dashboard d'administrer ce template
# Cette clé donne accès complet aux endpoints /api/export/* 
TEMPLATE_MASTER_API_KEY=template-master-key-2025

# Clé secrète Flask pour les sessions (IMPORTANT: utilisez une valeur unique en production)
# Vous pouvez générer une clé sécurisée avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
FLASK_SECRET=votre_cle_secrete_tres_longue_et_aleatoire
# Ou utilisez:
# SECRET_KEY=votre_cle_secrete_tres_longue_et_aleatoire

# Email de l'administrateur principal (optionnel, défaut: coco.cayre@gmail.com)
ADMIN_EMAIL=votre.email@example.com

# ==============================================
# CONFIGURATION BASE DE DONNÉES (PRODUCTION)
# ==============================================
# Pour PostgreSQL sur Scalingo/Render, définissez cette variable
# Scalingo/Render la fournissent automatiquement, vous n'avez pas besoin de la définir manuellement
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Si DATABASE_URL n'est pas définie, l'application utilisera SQLite en local (paintings.db)

# ==============================================
# CONFIGURATION STRIPE (Optionnel)
# ==============================================
# Ces valeurs peuvent aussi être dans la table settings de la BDD
# STRIPE_SECRET_KEY=sk_test_... (côté serveur uniquement)
# STRIPE_PUBLISHABLE_KEY=pk_test_... (peut être exposé côté client)

# ==============================================
# CONFIGURATION SMTP (Optionnel)
# ==============================================
# Ces valeurs sont d'abord recherchées dans les variables d'environnement,
# puis dans la table settings de la BDD
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=votre.email@gmail.com
MAIL_PASSWORD=votre_mot_de_passe_application_gmail

# Ou utilisez ces noms alternatifs:
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre.email@gmail.com
# SMTP_PASSWORD=votre_mot_de_passe

# ==============================================
# CONFIGURATION DASHBOARD (Optionnel)
# ==============================================
# URL du dashboard central pour la synchronisation
# DASHBOARD_URL=https://admin.artworksdigital.fr

# ==============================================
# NOTES D'UTILISATION
# ==============================================
# 1. Copiez ce fichier en .env (sans .example)
# 2. Remplissez les valeurs nécessaires
# 3. Ne commitez JAMAIS le fichier .env sur Git
# 4. Sur Scalingo/Render, définissez ces variables dans l'interface web
# 5. Les variables d'environnement ont la priorité sur les valeurs en BDD
# 6. Toutes les valeurs sont optionnelles avec des fallbacks raisonnables
