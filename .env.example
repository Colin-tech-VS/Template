# Exemple de configuration des variables d'environnement

# ==============================================
# CLÉS API ET SÉCURITÉ
# ==============================================
# Clé API maître pour permettre au dashboard d'administrer ce template
# Cette clé donne accès complet aux endpoints /api/export/* 
TEMPLATE_MASTER_API_KEY=template-master-key-2025

# Clé secrète Flask (OBLIGATOIRE en production)
# Générez une clé forte avec: python -c "import secrets; print(secrets.token_urlsafe(32))"
# FLASK_SECRET=votre_cle_secrete_tres_longue_et_aleatoire
# Ou utilisez SECRET_KEY comme alternative
# SECRET_KEY=votre_cle_secrete_tres_longue_et_aleatoire

# ==============================================
# CONFIGURATION BASE DE DONNÉES (PRODUCTION)
# ==============================================
# Pour PostgreSQL sur Render, définissez cette variable
# Render la fournit automatiquement, vous n'avez pas besoin de la définir manuellement
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Si DATABASE_URL n'est pas définie, l'application utilisera SQLite en local (paintings.db)

# ==============================================
# CONFIGURATION STRIPE (Optionnel)
# ==============================================
# Ces valeurs peuvent aussi être dans la table settings de la BDD
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...

# ==============================================
# CONFIGURATION SMTP (Optionnel)
# ==============================================
# Ces valeurs sont stockées dans la table settings
# Mais vous pouvez les définir ici pour les surcharger
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=votre.email@gmail.com
MAIL_PASSWORD=votre_mot_de_passe_application

# ==============================================
# CONFIGURATION ADMINISTRATEUR
# ==============================================
# Email de l'administrateur principal (qui ne peut pas perdre ses droits)
ADMIN_EMAIL=admin@example.com

# ==============================================
# NOTES D'UTILISATION
# ==============================================
# 1. Copiez ce fichier en .env (sans .example)
# 2. Remplissez les valeurs nécessaires
# 3. Ne commitez JAMAIS le fichier .env sur Git
# 4. Sur Render, définissez ces variables dans l'interface web
# 5. Pour Gmail, utilisez un "mot de passe d'application" (2FA requis)
