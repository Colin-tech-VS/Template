Fix: remove duplicate get_stripe_publishable_key function definition

This was causing an AssertionError when Flask tried to map both routes to
the same endpoint name. Kept the new version with improved response format.
